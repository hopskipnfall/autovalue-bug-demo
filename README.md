Generated by `mvn clean compile`:

```java
package com.autovaluebug;

// Generated by com.google.auto.value.processor.AutoValueProcessor
final class AutoValue_MyAutoValueClass extends MyAutoValueClass {

  private final int gamma;

  private final byte alpha;

  private final int beta;

  AutoValue_MyAutoValueClass(
      int gamma,
      byte alpha,
      int beta) {
    this.gamma = gamma;
    this.alpha = alpha;
    this.beta = beta;
  }

  @Override
  public int gamma() {
    return gamma;
  }

  @Override
  public byte alpha() {
    return alpha;
  }

  @Override
  public int beta() {
    return beta;
  }

  @Override
  public String toString() {
    return "MyAutoValueClass{"
        + "gamma=" + gamma + ", "
        + "alpha=" + alpha + ", "
        + "beta=" + beta
        + "}";
  }

  @Override
  public boolean equals(Object o) {
    if (o == this) {
      return true;
    }
    if (o instanceof MyAutoValueClass) {
      MyAutoValueClass that = (MyAutoValueClass) o;
      return this.gamma == that.gamma()
          && this.alpha == that.alpha()
          && this.beta == that.beta();
    }
    return false;
  }

  @Override
  public int hashCode() {
    int h$ = 1;
    h$ *= 1000003;
    h$ ^= gamma;
    h$ *= 1000003;
    h$ ^= alpha;
    h$ *= 1000003;
    h$ ^= beta;
    return h$;
  }

}
```

Generated by saving in vscode and letting the annnotation processor output the implementation class:

```java
package com.autovaluebug;

// Generated by com.google.auto.value.processor.AutoValueProcessor
final class AutoValue_MyAutoValueClass extends MyAutoValueClass {

  private final byte alpha;

  private final int gamma;

  private final int beta;

  AutoValue_MyAutoValueClass(
      byte alpha,
      int gamma,
      int beta) {
    this.alpha = alpha;
    this.gamma = gamma;
    this.beta = beta;
  }

  @Override
  public byte alpha() {
    return alpha;
  }

  @Override
  public int gamma() {
    return gamma;
  }

  @Override
  public int beta() {
    return beta;
  }

  @Override
  public String toString() {
    return "MyAutoValueClass{"
        + "alpha=" + alpha + ", "
        + "gamma=" + gamma + ", "
        + "beta=" + beta
        + "}";
  }

  @Override
  public boolean equals(Object o) {
    if (o == this) {
      return true;
    }
    if (o instanceof MyAutoValueClass) {
      MyAutoValueClass that = (MyAutoValueClass) o;
      return this.alpha == that.alpha()
          && this.gamma == that.gamma()
          && this.beta == that.beta();
    }
    return false;
  }

  @Override
  public int hashCode() {
    int h$ = 1;
    h$ *= 1000003;
    h$ ^= alpha;
    h$ *= 1000003;
    h$ ^= gamma;
    h$ *= 1000003;
    h$ ^= beta;
    return h$;
  }

}
```